import React from 'react';
import {StyleSheet} from 'react-native';

const senStyle = StyleSheet.create({
  flex: 1,
  justifyContent: 'center',
  alignItems: 'center',
  alignContent: 'center',
});

export default senStyle;
